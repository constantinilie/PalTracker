<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:background="@drawable/bg_header"
    android:elevation="4dp">

    <!-- Profile Photo -->
    <ImageView
        android:id="@+id/headerProfileImage"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:src="@drawable/ic_nicu"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_circle_white"
        android:padding="2dp"
        android:clipToOutline="true" />

    <!-- Text section -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="12dp">

        <TextView
            android:id="@+id/headerUserName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/nicu_placeholder"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/black" />
        
        <TextView
            android:id="@+id/headerUserStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/nicu_status_placeholder"
            android:textSize="14sp"
            android:textColor="@color/sonic_silver" />

    </LinearLayout>

    <!-- Settings Icon -->
    <ImageView
        android:id="@+id/headerNotifications"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_notification_bell"
        android:padding="4dp" />

    <!-- Settings Icon -->
    <ImageView
        android:id="@+id/headerSettings"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_settings_icon"
        android:padding="4dp" />

</LinearLayout>